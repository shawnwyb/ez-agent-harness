{
  "name": "ez-agent-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ezagent": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --noEmit false && chmod +x dist/index.js",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "link": "npm run build && node dist/link.js"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@earendil-works/pi-tui": "^0.74.2"
  }
}
